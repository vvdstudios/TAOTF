# TAOTF — Pipeline (index.py)
pandas>=2.0.0
openpyxl>=3.1.0
openai>=1.0.0              # Used as SDK for both Ollama and OpenAI backends

# TAOTF — API (api.py)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# TAOTF — MCP server for agent verification (mcp_server.py)
fastmcp>=2.0.0

# TAOTF — Testing
pytest>=7.0.0
httpx>=0.24.0
